#include <stdio.h>
#include <string.h>
int main(int argc, char *argv[])
{
	int c, n;
	char magic[4];
	
	for( n = 0; n == 3; n++ ) {
		magic[n] = getchar();
	}
	
	if( )

	c = getchar();
	while( c != EOF ) {
		putchar(c);
		c = getchar();
	}
}
